# ── AI Provider ──────────────────────────────────────────────────────────────
# Which AI provider to use. Supported values:
#   anthropic    (default) — Anthropic Claude
#   openai                 — OpenAI GPT models
#   azure-openai           — Azure OpenAI Service
#   ollama                 — Local Ollama (OpenAI-compatible)
#   gemini                 — Google Gemini
AI_PROVIDER=anthropic

# Your API key for the chosen provider
# Anthropic:    sk-ant-...   (https://console.anthropic.com)
# OpenAI:       sk-...       (https://platform.openai.com)
# Gemini:       AI...        (https://aistudio.google.com)
# Azure OpenAI: your Azure key
# Ollama:       leave empty or set to "ollama" (no key needed)
AI_API_KEY=

# Model name for the chosen provider. Examples:
#   Anthropic:    claude-3-5-sonnet-20241022  claude-3-haiku-20240307
#   OpenAI:       gpt-4o  gpt-4o-mini
#   Gemini:       gemini-1.5-flash  gemini-1.5-pro
#   Ollama:       llama3.2  mistral  phi3
#   Azure OpenAI: your deployment name
AI_MODEL=claude-3-5-sonnet-20241022

# (Optional) Custom API endpoint — required for Ollama and Azure OpenAI
# Ollama example:       http://localhost:11434/v1
# Azure OpenAI example: https://<resource>.openai.azure.com/openai/deployments/<deployment>
# AI_API_ENDPOINT=

# (Optional) Azure OpenAI API version — required only for Azure OpenAI
# AI_API_VERSION=2024-02-01

# (Optional) Max tokens for AI response (default: 2000)
# AI_MAX_TOKENS=2000

# ── Access Control ───────────────────────────────────────────────────────────
# Comma-separated list of invite codes in XXX-XXX format.
# Share these with people you want to give access to your profile.
# Example: ABC-123,DEF-456,GHI-789
ACCESS_CODES=

# ── Profile Content ──────────────────────────────────────────────────────────
# If your hosting platform limits env var size (e.g. Azure SWA caps at 10KB),
# deploy api/profile.md as a file instead of using this variable.
# Only needed if you cannot deploy files alongside your function.
# PROFILE_PROMPT=
